CREATE TABLE ReadPosts(
  id TEXT NOT NULL PRIMARY KEY
);

isPostRead:
SELECT EXISTS(
  SELECT 1
  FROM ReadPosts
  WHERE id = ?
) AS isRead;

selectAllPosts:
SELECT *
FROM ReadPosts;

markRead:
INSERT OR IGNORE
INTO ReadPosts(id)
VALUES (?);

markUnread:
DELETE FROM ReadPosts
WHERE id = ?;
